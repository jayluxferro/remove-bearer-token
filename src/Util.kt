package burp


class Util {
    companion object {
        const val appName = "Remove Bearer Token"
        const val appVersion = "1.0.0"
        val headers = arrayListOf("Authorization", "Bearer")
    }
}
